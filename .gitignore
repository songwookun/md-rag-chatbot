# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env.*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

architecture.html

# 작업 노트 — 공개 대상 아님 (블로그 원재료)
#   디렉터리째 제외하면 하위 예외(!)가 안 먹으므로 /docs/* 로 쓴다
/docs/*
# 단, 실험 기록은 공개한다 — 코드의 상수들이 근거로 참조하는 문서다
!/docs/experiments/

# Claude 메모리 / 로컬 설정 — 공개 대상 아님
.claude/

# 임베딩 캐시 (용량 큼, 재생성 가능)
notebooks/data/embed_cache.pkl

# 파이썬 캐시
__pycache__/
*.pyc
